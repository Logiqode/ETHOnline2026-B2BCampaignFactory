# ==========================================================================
# CRE PROJECT SETTINGS FILE
# ==========================================================================
# This workflow verifies campaign eligibility in the enclave, then writes
# the verdict on-chain via evm.write — Base Sepolia (chain id 84532).
staging-settings:
  rpcs:
    - chain-name: ethereum-testnet-sepolia-base-1
      url: https://base-sepolia-rpc.publicnode.com

# ==========================================================================
production-settings:
  rpcs:
    - chain-name: ethereum-testnet-sepolia-base-1
      url: https://base-sepolia-rpc.publicnode.com
